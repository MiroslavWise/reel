generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Reel {
  id         String   @id @db.Uuid
  telegramId BigInt   @map("telegram_id")
  name       String
  users      Json     @default("[]")

  @@map("reels")
}